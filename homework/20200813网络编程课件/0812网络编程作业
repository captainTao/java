作业：
TCP/IP的4层模型是什么？分别负责什么功能？有几种模式，分别是什么？
UDP、TCP的差异有哪些？
例举一个ipv4、iPv6的ip地址；
编程题：客户端文件上传，保证服务端能循环接收文件，并且能够正常保存到服务器
编程题：浏览器请求index.html时，服务端返回一个带有当前时间的网页，网页上显示当前时间（html内容在服务器端自动生成，不能以读取一个html文件并返回的形式实现）
1、TCP/IP的四层结构是什么？
答：（1）数据链路层，网络层，传输层，应用层。
        数据链路层：实现网卡接口的网络驱动程序，以处理数据在物理媒介上的传输。
        网络层：实现数据包的选路和转发。
        传输层：为两台主机上的应用程序提供端到端的通信。
        应用层：负责处理应用程序的逻辑。
    （2）OSI参考模型：物理层，数据链路层，网络层，传输层，会话层，表示层，应用层。
        物理层：利用传输介质为数据链路层提供物理连接，实现比特流的透明传输。
        数据链路层：通过各种协议，将由差错的物理信道变为无差错的、能可靠传输数据帧的数据链路。
        网络层：通过路由算法，为报文或分组通过通信子网选择最适当的路径。
        传输层：向高层屏蔽下层数据通信的具体细节，即向用户透明的传送报文。
        会话层：用户应用程序和网络之间的接口。
        表示层：处理用户信息的表示问题，如编码，数据格式转换和加解密
        应用层：直接向用户提供服务，完成用户希望在网络上完成的各种工作。
2、UDP、TCP的差异？
答：（1）TCP面向连接，UDP是无连接的，发送数据之前不需要建立连接
   （2）TCP提供可靠的服务，UDP尽最大努力交付，不保证可靠交付
   （3）TCP面向字节流，实际上是TCP把数据看成一连串无结构的字节流，UDP是面向报文的
   （4）每一条TCP连接只能是点到点的，UDP支持一对一，一对多，多对一和多堆垛的通信
   （5）TCP首部消耗20字节，UDP的首部开销小只有8哥字节
   （6）TCP的逻辑通信信道是全双工的可靠信道，UDP的时不可靠信道。
3、ipv4，ipv6的地址。
答：ipv4长度为32位（4字节），由网络和主机部分组成，根据地址的前几位，可定义各种地址类：A、B、C、D、E类。例如10.1.18.112就是一个ipv4的地址。
   ipv6长度为128位（16字节），基本体系结构的网络数字为64位，主机数字为64位。ipv6的主机部分将派生自MAC地址或其他接口表示。例如：ffff：10.120.78.40就是一个ipv6地址。
